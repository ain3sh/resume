{
    "ignoreCommand": "! git diff --quiet HEAD^ HEAD -- pdfs .github README.md build",
    "headers": [
        {
            "source": "/(.*)",
            "headers": [
                { "key": "X-GitHub-Actions", "value": "true" }
            ]
        }
    ]
}